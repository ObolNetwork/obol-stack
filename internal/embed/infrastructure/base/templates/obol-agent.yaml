---
# Agent namespace — retained for backward compatibility.
# The default obol-agent Hermes instance runs in hermes-obol-agent namespace;
# RBAC is managed by the openclaw-monetize-read/workload ClusterRoles.
apiVersion: v1
kind: Namespace
metadata:
  name: agent
