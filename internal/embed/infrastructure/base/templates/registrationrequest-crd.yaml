---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: registrationrequests.obol.org
spec:
  group: obol.org
  names:
    kind: RegistrationRequest
    listKind: RegistrationRequestList
    plural: registrationrequests
    shortNames:
    - rr
    singular: registrationrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serviceOfferName
      name: Offer
      type: string
    - jsonPath: .spec.desiredState
      name: State
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.agentId
      name: AgentID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RegistrationRequest isolates ERC-8004 publication and on-chain side
          effects from the main ServiceOffer reconciliation loop. ServiceOffer
          remains the source of truth.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              chain:
                description: ERC-8004 registration chain alias for this request.
                type: string
              desiredState:
                enum:
                - Active
                - Tombstoned
                type: string
              serviceOfferName:
                type: string
              serviceOfferNamespace:
                type: string
            required:
            - desiredState
            - serviceOfferName
            - serviceOfferNamespace
            type: object
          status:
            properties:
              agentId:
                type: string
              message:
                type: string
              metadataSynced:
                type: boolean
              phase:
                type: string
              publishedUrl:
                type: string
              registrationOwner:
                type: string
              registrationSearchFromBlock:
                format: int64
                type: integer
              registrationTxHash:
                type: string
              registrationUri:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
