repositories:
  - name: ingress-nginx
    url: https://kubernetes.github.io/ingress-nginx

releases:
  # Nginx Ingress Controller (official kubernetes project)
  - name: ingress-nginx
    namespace: ingress
    createNamespace: true
    chart: ingress-nginx/ingress-nginx
    version: 4.13.3
    values:
      - values.yaml
