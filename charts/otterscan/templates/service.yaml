apiVersion: v1
kind: Service
metadata:
  name: {{ include "otterscan.fullname" . }}
  labels:
    {{- include "otterscan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otterscan.selectorLabels" . | nindent 4 }}
