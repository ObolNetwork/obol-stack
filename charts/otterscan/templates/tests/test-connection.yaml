apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otterscan.fullname" . }}-test-connection"
  labels:
    {{- include "otterscan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otterscan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
